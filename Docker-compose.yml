version: "3.9"

services:
  websocket-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080" # Ajusta este puerto si tu aplicación escucha en otro puerto.
    environment:
      - NODE_ENV=production
    restart: always
